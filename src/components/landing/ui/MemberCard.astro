---
import type { ImageMetadata } from "astro";
import Image from "astro/components/Image.astro";
import GithubIcon from "../../../assets/icons/github-mark.svg";
import WebIcon from "../../icons/WebIcon.astro";
import type { Team } from "../../../utils/types";

interface Props {
    name: string;
    title: string;
    position: string;
    website: string;
    github: string;
    headshot: ImageMetadata;
    teams: Team[];
}

const { name, title, position, website, github, headshot, teams } = Astro.props;
---

<div class="text-center mx-auto">
    <Image
        class="rounded-full shadow-xl mb-1 aspect-square object-cover border border-gray-200"
        src={headshot}
        alt={`${name}`}
    />
    <div class="mt-2">
        <h5 class="text-sm sm:text-lg">
            {name}
        </h5>
        <p class="text-sm sm:text-base -mb-1 font-semibold">
            {position}
        </p>
        <p class="text-sm sm:text-base mb-1">
            {title}
        </p>
        <div class="flex items-center justify-center gap-4 mt-1">
            {
                github && (
                    <a href={github} target="_blank" rel="noopener noreferrer">
                        <Image
                            height="20"
                            width="20"
                            src={GithubIcon}
                            alt="GitHub Icon"
                        />
                    </a>
                )
            }
            {
                website && (
                    <a
                        href={website}
                        aria-label={`${name}'s personal website`}
                        target="_blank"
                        rel="noopener noreferrer">
                        <WebIcon />
                    </a>
                )
            }
        </div>
    </div>
</div>
