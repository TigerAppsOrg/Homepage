---
interface Props {
    title: string;
    description: string;
    note: string;
    webLink: string;
    repoLink: string;
    screenshot: string;
}

const { title, description, note, webLink, repoLink, screenshot } = Astro.props;
---
